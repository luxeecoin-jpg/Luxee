// Legacy Firebase seed file - no longer used.
// Products and hero slides are now managed via the Admin Dashboard + PostgreSQL (Prisma).
// See prisma/seed.ts for database seeding.

export const seedDatabase = async () => {
  console.warn('seedDatabase is deprecated. Use the Admin Dashboard to manage products.');
};
